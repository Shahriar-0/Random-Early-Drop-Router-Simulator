#include "worker.h"

Worker::Worker(double minTh, double maxTh, double maxP, double wq, int duration)
    : _minTh(minTh), _maxTh(maxTh), _maxP(maxP), _wq(wq), _duration(duration) {}

void Worker::run() {
    // TODO
}
